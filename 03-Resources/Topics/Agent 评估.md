---
tags:
  - ai
  - agent
  - evaluation
type: resource
status: active
created: 2026-04-14
updated: 2026-04-20
canonical: true
aliases:
  - 大模型与 Agent 测试方法：从结果评估到过程评估
---

# Agent 评估

## Summary

Agent 评估的重点不只是最终答案对不对，而是任务是否完成、过程是否健康、改动是否真的带来整体提升。

## 这页解决什么问题

这页用来建立一个稳定评估框架，避免把 Agent 质量误判成“试了几个例子感觉还行”。

## 选用原则

收录到这页的内容，优先满足以下条件：

- 能帮助区分结果评估、过程评估、对比实验和生产反馈
- 能把“回答器评测”与“任务执行系统评测”区分开
- 能支持持续迭代，而不是一次性打分

## 核心内容

### 为什么 Agent 评估和模型评测不同

一旦系统开始调用检索、工具、工作流或多代理机制，评估对象就从“回答质量”变成“任务执行系统质量”。

只看最终答案会掩盖三个问题：

- 结果正确，但过程低效或脆弱
- 结果错误，却不知道失败发生在哪一层
- 小改 prompt、模型或工具配置后，整体行为已变化但无从感知

### 四层评估框架

1. 结果评估：最终是否完成任务、是否相关、是否忠实、是否有幻觉
2. 过程评估：工具是否选对、顺序是否合理、是否存在冗余步骤
3. 对比实验：比较不同 prompt、模型、工具配置或 agent 结构
4. 生产反馈：线上监控、人工反馈、回退检测、长期质量漂移

### 结果评估回答什么问题

如果只看最终交付物，这个系统对用户是否可用？

适合：

- RAG 问答
- 摘要、分类、抽取
- 单轮或短链路 Agent

### 过程评估回答什么问题

这个 Agent 的做事方式是否合理？

重点看：

- 工具调用正确性
- 轨迹与参考流程是否偏差过大
- 是否在错误节点循环
- 是否存在不必要的步骤和成本

如果没有 trace / trajectory 数据，这一层通常会退化成“凭感觉猜哪里坏了”。

### 对比实验的价值

评估不应停留在“绝对好不好”，还要能比较：

- 不同模型
- 不同 system prompt
- 不同检索策略
- 单 Agent 与多 Agent

更稳的做法不是直接上线上分流，而是先做离线 A/B：

- 固定 dataset
- 固定评判标准
- 同时比较结果指标和过程指标
- 先筛掉明显退化版本，再考虑线上试点

### 生产反馈的必要性

离线数据集无法完全覆盖真实流量。真正稳定的 Agent 系统，需要线上监控质量、成本、延迟和失败类型。

### Agent 时代最关键的变化

过去一条样本更像：

`input -> output`

现在更像：

`input -> planning -> tool calls -> intermediate steps -> final output`

这意味着评估对象已经从“答案”扩展成“执行轨迹”。

真正常见的失败，不只是答错，还包括：

- 做了不该做的动作
- 漏了必须做的动作
- 路径过长
- 在错误节点重复重试
- 每一层单看都没错，但组合起来出错

## Sources

- 来源：旧库《小宇宙》中的 Agent 评估迁移稿，已并入本页

## Related

- [[AI 可观测性]]
- [[Agent 编排与 Sub-agent]]
- [[模型测试工具选型]]

## Open Questions

- 对你当前最常用的 Agent 场景，哪一层评估最缺失？
- 什么时候过程评估的成本会超过它带来的诊断价值？
- 哪些线上回退信号最值得最先纳入监控？
